# Документація про структуру проекту

## 📁 Опис проекту

Цей документ описує структуру FastAPI проекту, який використовує SQLAlchemy для роботи з базою даних, Pydantic для валідації даних, та Jinja2 для рендерингу HTML шаблонів.

## 📂 Структура папок

```
proshield/
│
├── api/
│   └── routes/
├── content/
│   ├── data/
│   └── *.py
├── core/
│   ├── config.py
│   └── database.py
├── crud/
├── db/
│   └── models/
├── schemas/
├── static/
│   ├── assets/
│   ├── css/
│   └── js/
├── templates/
├── views/
└── main.py
```

## 📄 Детальний опис

### 1. 📁 `api/routes/`

**Призначення:** Зберігання REST API ендпоінтів

Ця папка містить модулі з API маршрутами (routes), які обробляють HTTP запити. Кожен файл зазвичай відповідає за окрему логічну групу ендпоінтів.

**Приклад структури:**

```
api/
└── routes/
    ├── __init__.py
    ├── attenuation_coefficients.py         # Г.1 - Коефіцієнт послаблення дози гамма-випромінювання та нейтронів проникаючої радіації товщею матеріалів
    ├── building_types.py                   # типи забудови
    ├── buildings_coefficients.py           # Г.3 - Коефіцієнт послаблення радіації огороджувальними конструкціями житлових, громадських та виробничих будівель
    ├── calculations.py                     # основні розрахунки
    ├── health.py                           # ендпоінт для перевірки чи ппоект запущений
    ├── location_condition_coefficients.py  # Г.2 - коефіцієнт, який враховує зниження дози проникаючої радіації у забудові
    ├── materials.py                        # матеріали
    ├── storage_classes.py                  # А.1 - Клас сховищ, СПП із захисними властивостями сховищ
    └── wall_materials.py                   # види матеріалу стін
```

### 2. 📁 `content/data/`

**Призначення:** Зберігання початкових даних у форматі JSON

Містить JSON файли з даними, які автоматично завантажуються в базу даних при старті проекту. Це дозволяє уникнути ручного внесення початкових даних.

**Приклад файлів:**

- `attenuation_coefficients.json`
- `building_types.json`
- `buildings_coefficients.json`

### 3. 📁 `content/`

**Призначення:** Python модулі для завантаження даних

Містить Python файли з функціями для завантаження даних з JSON файлів у відповідні таблиці бази даних. Кожен Python файл відповідає за обробку конкретного JSON файлу.

**Типова структура файлу:**

```python
# content/load_users.py
def upload_attenuation_coefficients(db_session):
    """
    Завантажує дані про коефіцієнт послаблення
    дози гамма-випромінювання та нейтронів
    проникаючої радіації товщею матеріалів
    """
    pass
```

### 4. 📁 `core/`

**Призначення:** Основні конфігурації та налаштування проекту

#### 📄 `config.py`

Містить конфігураційні параметри проекту:

- Налаштування бази даних
- Секретні ключі
- API налаштування
- Змінні середовища

**Приклад використання:**

```python
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    model_config = SettingsConfigDict(
        env_file=".env",
        env_ignore_empty=True,
        extra="ignore",
    )

    # api version
    API_V1_STR: str = "/api/v1"

    # environment
    ENVIRONMENT: str = ENVIROMENT.DEVELOPMENT

    # database settings
    DATABASE_URL: str = ""
```

#### 📄 `database.py`

Налаштування підключення до бази даних:

- Створення engine для SQLAlchemy
- Налаштування сесій
- Функція `get_db()` для dependency injection
- Обробка помилок підключення

**Ключові компоненти:**

- `engine` - об'єкт для підключення до БД
- `SessionLocal` - фабрика сесій
- `get_db()` - генератор для отримання сесії БД

### 5. 📁 `crud/`

**Призначення:** CRUD операції (Create, Read, Update, Delete)

Містить модулі з функціями для роботи з базою даних. Реалізує бізнес-логіку взаємодії з даними.

**Типова структура:**

```python
# crud/user.py
def get_material_thickness(
    db: Session,
    material_id: int,
) -> list[int]:
    pass

def get_building_coefficient_by_id(
    db: Session,
    building_coefficient_id: int,
) -> Optional[models.BuildingCoefficient]:
    pass
```

### 6. 📁 `db/models/`

**Призначення:** SQLAlchemy моделі таблиць БД

Містить класи Python, які представляють таблиці бази даних через SQLAlchemy ORM.

**Приклад моделі:**

```python
lass AttenuationCoefficient(Base):
    """
    Табоиця коефіцієнтів послаблення дози гамма-випромінювання
    та нейтронів проникаючої радіації товщею матеріалів
    """

    __tablename__ = "attenuation_coefficients"

    id: Mapped[int] = mapped_column(primary_key=True)
    # матеріал
    material_id: Mapped[int] = mapped_column(ForeignKey("materials.id"))
    # товщина шару матеріалу
    material_thickness: Mapped[int] = mapped_column()
    # щільність матеріалу
    material_density: Mapped[float] = mapped_column()
    # коефіцієнт послаблення дози нейтронів огороджувальною конструкцією
    neutron_dose_coefficient: Mapped[float] = mapped_column()
    # коефіцієнт послаблення дози гамма-випромінювання огороджувальною конструкцією
    gamma_dose_coefficient: Mapped[float] = mapped_column()

    material: Mapped["Material"] = relationship(
        "Material",
        foreign_keys="AttenuationCoefficient.material_id",
    )

    @property
    def material_name(self) -> str:
        return self.material.name
```

### 7. 📁 `schemas/`

**Призначення:** Pydantic моделі для валідації даних

Містить схеми для валідації вхідних даних та серіалізації вихідних даних API.

**Типи схем:**

- **Request schemas** - валідація вхідних даних
- **Response schemas** - форматування відповідей
- **Base schemas** - спільні атрибути

**Приклад:**

```python
from typing import Optional

from pydantic import BaseModel


class AttenuationCoefficientCreate(BaseModel):
    material_id: int
    material_thickness: int
    material_density: float
    neutron_dose_coefficient: float
    gamma_dose_coefficient: float


class AttenuationCoefficient(AttenuationCoefficientCreate):
    id: int
    material_name: str


class AttenuationCoefficientUpdate(BaseModel):
    neutron_dose_coefficient: Optional[float] = None
    gamma_dose_coefficient: Optional[float] = None

```

### 8. 📁 `static/`

**Призначення:** Статичні файли для веб-інтерфейсу

#### Підпапки:

- **`assets/`** - зображення, іконки, шрифти
- **`css/`** - стилі CSS
- **`js/`** - JavaScript файли

Використовується для обслуговування статичного контенту у Jinja2 шаблонах.

### 9. 📁 `templates/`

**Призначення:** HTML шаблони Jinja2

Містить HTML шаблони для рендерингу веб-сторінок. Підтримує наслідування шаблонів та динамічне вставлення даних.

**Типова структура:**

```
templates/
├── main/           # Базовий шаблон
├── shelter/        # Шаблони для `Захисні споруди цивільного захисту`
└── square-law/     # Шаблони для `Розрахунок радіаційного захисту за квадратичним законом`
```

### 10. 📁 `views/`

**Призначення:** API ендпоінти для frontend

Містить контролери, які обробляють запити від фронтенду та повертають HTML сторінки або дані для рендерингу. На відміну від `api/routes/`, які повертають JSON, ці ендпоінти працюють з шаблонами.

**Приклад:**

```python
from fastapi import APIRouter, Depends, Request
from fastapi.responses import HTMLResponse
from fastapi.staticfiles import StaticFiles
from proshield.views.dependencies import get_templates

router = APIRouter()


@router.get("/", response_class=HTMLResponse)
async def home_view(request: Request, templates=Depends(get_templates)):
    return templates.TemplateResponse(
        request=request, name="main/home.html", context={}
    )
```
